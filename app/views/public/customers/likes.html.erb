<div class="container">
  <div class="row">
    <div class="col-md-10 mx-auto my-5">
      <h4>likes index</h4>

      <div class="row">
        <div class="col-md-8">
          <% @like_posts.each do |post| %>
            <%= image_tag post.image, width: "30%" %>
            <%= post.title %>
          <% end %>
        </div>
      </div>

    </div>
  </div>
</div>